
  // fetchSessionJSON().then((session) => {
  //   session = session.filter((item) => {
  //     return item.Sessions.filter((i) => i.id == selectedMovie.id) != [];
  //   });
  //   setSession(session);
  // });